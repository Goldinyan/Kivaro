package main.java.ui.animations;

import main.java.ui.*;


public class ManualPanelAnimator {
   /* private final UserManual manual;
    private final ManualButton button;
    private final JFrame frame;

    private float progress = 0f;
    private boolean visible = false;

    public ManualPanelAnimator(UserManual manual, ManualButton button, JFrame frame) {
        this.manual = manual;
        this.button = button;
        this.frame = frame;
    }

    public void toggle() {
        visible = !visible;
        timer.start();
    }

    private final Timer timer = new Timer(8, e -> {
        // deine Animation hier rein
    });

    public void init() {
        manual.setVisible(false);
        frame.getLayeredPane().add(manual, JLayeredPane.POPUP_LAYER);
        frame.getLayeredPane().add(button, JLayeredPane.POPUP_LAYER);
    } */
}
